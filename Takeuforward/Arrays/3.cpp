#include <bits/stdc++.h>
using namespace std;

void duplicates_no(int arr[] , int n){
    int i = arr[0];
    int j = arr[1];

    for(int k = 0; k < n; k++){
        j = arr[k+1];
        if(arr[k] ){

        }
    }
}

    int
    main()
{
    int arr[6];

    cout << "Give the 6 inputs for the array: ";

    for (int i = 0; i < 6; i++)
    {
        cin >> arr[i];
    }

    duplicates_no(arr, 6);

    return 0;
}